rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /geekbench_scores/{model} {
      allow read: if true;
      allow write: if false; // Only admin (functions) can write
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}